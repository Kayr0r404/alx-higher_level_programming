"""
    Prints text with 2 new lines after each '.', '?', and ':' character.

    Args:
    @text: The input text string.

    Returns:
    None, it prints the formatted text.

    Raises:
    TypeError: If the input is not a string.

    Examples:
    >>> text_indentation = __import__("5-text_indentation").text_indentation
    >>> text = "This is a sentence. It has some punctuation: like periods, questions? and colons."
    >>> text_indentation(text)
    This is a sentence.
    It has some punctuation:
    like periods, questions?
    and colons.
    
    >>> text = "No punctuation in this text"
    >>> text_indentation(text)

    >>> text = 42
    >>> text_indentation(text)
    Traceback (most recent call last):
    ...
    TypeError: text must be a string

    >>> text = ["List", "of", "strings"]
    >>> text_indentation(text)
    Traceback (most recent call last):
    ...
    TypeError: text must be a string

    """